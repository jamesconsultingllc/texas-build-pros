# =============================================================================
# Legacy Builders - Environment Variables
# =============================================================================
# Copy this file to .env.local and fill in your values
# .env.local is gitignored and won't be committed
# =============================================================================

# Azure Configuration (Already set)
AZURE_SUBSCRIPTION_ID=3f3156c6-94bc-4752-a9d7-0d43fe99c92a
AZURE_TENANT_ID=a2bc6fb5-fba9-40b4-9ecc-2acf61cae876

# =============================================================================
# Application Insights (Required for Telemetry)
# =============================================================================
# Get connection string by running:
#   PowerShell: .\scripts\create-app-insights.ps1
#   Bash:       ./scripts/create-app-insights.sh
#   Bicep:      az deployment group show --resource-group legacy-builders-rg --name main --query properties.outputs.appInsightsConnectionString.value -o tsv
#
# Or manually from Azure Portal:
#   Azure Portal -> Application Insights -> Properties -> Connection String
# =============================================================================

VITE_APPINSIGHTS_CONNECTION_STRING=

# Example format (yours will be different):
# VITE_APPINSIGHTS_CONNECTION_STRING=InstrumentationKey=12345678-1234-1234-1234-123456789012;IngestionEndpoint=https://southcentralus-1.in.applicationinsights.azure.com/;LiveEndpoint=https://southcentralus.livediagnostics.monitor.azure.com/;ApplicationId=12345678-1234-1234-1234-123456789012

# =============================================================================
# Quick Setup
# =============================================================================
# Option 1: Using Bicep (Recommended)
#    1. Deploy infrastructure:
#       az group create --name legacy-builders-rg --location southcentralus
#       az deployment group create --resource-group legacy-builders-rg --template-file infrastructure/main.bicep
#
#    2. Get connection string:
#       az deployment group show --resource-group legacy-builders-rg --name main --query properties.outputs.appInsightsConnectionString.value -o tsv
#
#    3. Paste it above
#
# Option 2: Using PowerShell Script
#    1. Run: .\scripts\create-app-insights.ps1
#    2. Copy the output connection string
#    3. Paste it above
#
# Then install and test:
#    npm install @microsoft/applicationinsights-web
#    npm run dev
# =============================================================================
